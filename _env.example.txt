# ============================================================
# Green Recruiters - Environment Configuration
# Copy this file to .env and fill in real values.
# Never commit the real .env file to version control.
# ============================================================

# ---------- Database (Neon PostgreSQL) ----------
DATABASE_URL=postgresql://username:password@your-neon-host/dbname?sslmode=require
DB_POOL_MIN=1
DB_POOL_MAX=10

# ---------- Sessions / Auth ----------
# Generate a strong random value, e.g.: python3 -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=change-this-to-a-long-random-secret
SESSION_TTL_HOURS=12
OTP_TTL_MINUTES=10

# ---------- SMTP (Zoho Mail initially, provider-independent) ----------
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
SMTP_USERNAME=your-email@yourdomain.com
SMTP_PASSWORD=your-smtp-password-or-app-password
SMTP_USE_TLS=true
SMTP_FROM_EMAIL=your-email@yourdomain.com
SMTP_FROM_NAME=Green Recruiters

# ---------- Storage (local disk on VPS) ----------
RESUME_STORAGE_PATH=/var/greenrecruiters/resumes
CMS_ASSET_PATH=/var/greenrecruiters/assets

# ---------- CORS ----------
# Comma-separated list of origins allowed to call the API.
# Leave empty if frontend and backend are served from the same origin.
ALLOWED_ORIGINS=https://greenrecruiters.org
